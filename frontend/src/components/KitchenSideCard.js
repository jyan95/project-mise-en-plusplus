import React from 'react';
import { Card } from 'semantic-ui-react';

const KitchenSideCard = props => {
  <Card
    onClick={props.showKitchenDetails}
    header={props.}
  />
}

export default KitchenSideCard;
